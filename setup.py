import setuptools

setuptools.setup(
    name="mlutils",
    version="0.0.1",
    description="ML utility functions for notebooks",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)